#!/usr/bin/env bash

# @file StringUtils.sh
# @brief Operations on String considering null as empty string
# @description
#     This implementation do reference to the [java apache implementation](http://commons.apache.org/proper/commons-lang/javadocs/api-3.1/org/apache/commons/lang3/StringUtils.html)
#
# ### Method Summary
# <!-- StringUtils.summaryTableBegin -->
# | Methods                  | Brief                                 |
# |--------------------------|---------------------------------------|
# |containsOnly|Checks if the CharSequence contains only certain characters.
# |endsWith|Check if a string ends with a specified suffix.
# |equals|Compares two string, returning true if they represent equal sequences of characters.
# |indexOf|Finds the first index of a substring occurence within another string.
# |indexOfAny| Search a string to find the first index of any character in the given set of characters.
# |isBlank|Checks if a string contains only whitespaces characters or is empty or not existing.
# |isEmpty|Checks if a string is empty ("") or not existing.
# |split|Splits the provided text into an array.
# |startsWith|Check if a CharSequence starts with a specified prefix.
# |substring|Gets a substring from the specified String.
# |trim|Remove whitespaces characters from both ends of the input string.
# <!-- StringUtils.summaryTableEnd -->

